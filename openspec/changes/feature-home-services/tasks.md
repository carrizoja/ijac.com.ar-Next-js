## 1. Featured Services Selection

- [x] 1.1 Add a minimal way to derive the homepage featured services from the shared catalog, using the selected service slugs as the source of truth.
- [x] 1.2 Update the homepage `Servicios` section to render only the four featured services instead of the full catalog.

## 2. Homepage Navigation And UX

- [x] 2.1 Ensure the homepage `Servicios` section keeps a clear CTA button to `/services` for users who want to browse the full catalog.
- [x] 2.2 Verify the homepage cards and surrounding copy still read clearly now that the section is a curated featured subset.

## 3. Verification

- [x] 3.1 Confirm the non-featured services remain available on `/services`.
- [x] 3.2 Run the relevant project check or build to verify the filtered homepage section works without regressions.
